version: "3"
services:
  authorizeService:
    image: authorize:latest
    container_name: authorize-server
    ports:
      - "8899:8899"
      - "18899:18899"
      - "8999:8999"
    networks:
      - rpcnetwork

networks:
  rpcnetwork:
    external: true
